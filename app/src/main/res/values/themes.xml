<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CountDownLooper" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_grey</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary_grey_light</item>
        <item name="colorSecondaryVariant">@color/primary_medium</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorOnPrimary</item>
        <item name="android:actionBarStyle">@style/ThemeActionBar</item>
        <item name="android:actionBarTheme">@style/ThemeActionBar</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/ThemeActionBar</item>
        <item name="actionBarTheme">@style/ThemeActionBar</item>
        <!-- Customize your theme here. -->
    </style>

    //Transparent Action Bar
    <style name="ThemeActionBar" parent="@style/Widget.AppCompat.Light.ActionBar">
        // Bkg
        <item name="android:background">?attr/colorSecondary</item>
        <item name="background">?attr/colorSecondary</item>
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/black</item>
        <item name="android:colorControlNormal" tools:ignore="NewApi">?colorControlNormal</item>
    </style>

    <style name="MyDenseOutlined" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="materialThemeOverlay">@style/MyThemeOverlayOutlinedDense</item>
        <item name="hintEnabled">false</item>
    </style>

    <style name="MyThemeOverlayOutlinedDense">
        <item name="editTextStyle">@style/MyTextInputEditText_outlinedBox_dense_h</item>
        <item name="colorOnSurface">@color/primary_grey_light</item>
    </style>

    <style name="MyTextInputEditText_outlinedBox_dense_h" parent="@style/Widget.MaterialComponents.TextInputEditText.OutlinedBox.Dense">
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
    </style>

    <style name="MyNumberPickerStyle">
        <item name="android:textColorPrimary">@color/black</item>
    </style>
</resources>